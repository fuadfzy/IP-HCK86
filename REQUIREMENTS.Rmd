---
title: "Requirement Fulfillment - TableTalk"
date: "2025-08-22"
output:
  github_document: default
---

## ✅ Daftar Requirement yang Terpenuhi + Contoh

1) Tema Aplikasi
- Status: Terpenuhi (Foods/Restaurant Ordering)
- Contoh: Pemesanan via QR meja, keranjang, pembayaran Midtrans, riwayat pesanan, AI asisten menu

2) GitHub Workflow (branch, commit, merge)
- Status: Terpenuhi
- Contoh: Pengembangan di branch `fuad-dev` dan push ke repo:
  - `origin fuad-dev` (IP-HCK86)
  - `tabletalk fuad-dev:main` (TableTalk)

3) Arsitektur Client-Server
- Status: Terpenuhi
- Contoh: Client (Vite + React) di `frontend/` berkomunikasi ke Server (Express REST API) di `backend/`

4) Server: REST API (CRUD), Testing ≥ 90% coverage, API Docs
- Status: Terpenuhi
- Contoh:
  - CRUD Orders: GET/POST/PUT/DELETE `/orders`
  - Sessions: POST `/sessions`, Menu: GET `/menu-items`
  - Testing: `backend/__tests__/api.test.js`, laporan coverage di folder `coverage/`, threshold 90% di `package.json`
  - API Docs: `backend/API_DOCS.md` dan ringkasan alur di `PRESENTATION.md`

5) Client: Vite + React (Components, Router, Redux)
- Status: Terpenuhi
- Contoh: Routes (`/session/:tableCode`, `/auth/callback`, `/payment/*`) di `frontend/src/App.jsx`; pages/components di `frontend/src/pages/*`, `frontend/src/components/*`

6) Redux sebagai Single Source of Truth (fetch via store)
- Status: Terpenuhi
- Contoh: Thunks `fetchOrders`, `createOrder`, `updateOrder`, `deleteOrder` di `frontend/src/features/orders/orderSlice.js`; konsumsi data di `frontend/src/pages/OrderHistoryPage.jsx` dengan `useSelector`

7) Fitur Utama Menarik
- Status: Terpenuhi
- Contoh:
  - AI Chatbot (OpenAI): `backend/routes/ai.js` + `frontend/src/pages/ChatPage.jsx`
  - Payment Gateway (Midtrans Snap + webhook): `backend/routes/payments.js`, `backend/routes/paymentsNotification.js`, `frontend/src/pages/PaymentPage.jsx`
  - Social Sign-In (Google OAuth): `backend/routes/auth.js` + `frontend/src/pages/AuthCallback.jsx`
  - QR Scan (kamera & URL): `frontend/src/components/QRScanner.jsx`, `frontend/src/pages/QRSessionPage.jsx`
  - Responsive UI: scanner fullscreen, layout mobile-first

8) Wajib: Social Media Sign In (Google)
- Status: Terpenuhi
- Contoh: `/auth/google` → `/auth/google/callback` menerbitkan JWT; `AuthCallback.jsx` menyimpan token & user ke `localStorage`

9) Wajib: ≥2 Third-Party API (server-client)
- Status: Terpenuhi
- Contoh:
  - OpenAI (wajib AI): package `openai`, endpoint `/ai/chat`, dipanggil dari `ChatPage.jsx`
  - Midtrans: package `midtrans-client`, endpoint `/payments` + webhook `/payments/notification`

10) Deploy Server & Client (Public)
- Status: Terpenuhi
- Contoh:
  - Backend: https://api.fuadfzy.space (AWS EC2 + PM2, CORS terkonfigurasi)
  - Frontend: https://tabletalk-2025.web.app (Firebase Hosting)
  - Redirect Midtrans: `backend/routes/paymentRedirect.js` mengarahkan ke `/payment/*` pada frontend (env `FRONTEND_URL`)

## 🔄 Alur Demo Singkat

- Scan QR meja → `POST /sessions` → `sessionId` tersimpan (localStorage)
- Login Google → JWT diterima di `/auth/callback` → menuju `/choice`
- Buat/Edit pesanan (Redux thunks) → endpoint `/orders`
- Bayar → `POST /payments` (Snap) → callbacks + webhook `/payments/notification`
- Cek status realtime → `OrderHistoryPage.jsx` auto-refresh tiap 5 detik
